{
  "name": "@rexera/agents",
  "version": "1.0.0",
  "description": "AI agent integration system for Rexera 2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "setup": "npm install",
    "test": "jest"
  },
  "dependencies": {
    "@rexera/types": "*",
    "@rexera/database": "*",
    "axios": "^1.6.0",
    "retry": "^0.13.1",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/retry": "^0.12.0",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.0",
    "typescript": "^5.2.0"
  },
  "files": [
    "dist"
  ]
}