// Export all HTTP method handlers for Vercel
export { GET, POST, PUT, PATCH, DELETE, OPTIONS } from '../src/app';

// Also export the default app for compatibility
export { default } from '../src/app';